Hello,

<% if @street_address %>
  This is a reminder that street sweeping for <%= @street_address %> (<%= @area.name %>) will begin tomorrow.
<% else %>
  This is a reminder that street sweeping for <%= @area.name %> will begin tomorrow.
<% end %>

<%= @dates.join(" / ") %>

Cheers,
<%= ENV["SITE_NAME"] %>

<%= @disclaimer %>

Unsubscribe: <%= @unsubscribe_url %>
