Your <%= ENV["SITE_NAME"] %> subscription for <%= @area.name %> has been canceled.

Each year, the City of Chicago redraws its sweeping areas, so we have to cancel subscriptions that weren't fully confirmed or didn't have a street address. Yours was one of those.

Re-subscribe: <%= ENV["SITE_URL"] %>

Day-before reminders | Map of your sweep zone | Always free
<%= render "shared/email_footer" %>
