<% if @neighbor_areas.any? -%>
Confirm your sweeping alerts

We received a request to subscribe <%= @email %> to street sweeping alerts for these areas:

- <%= @formatted_address_area %>
<% @neighbor_areas.each do |neighbor_area| -%>
- <%= neighbor_area.name %>
<% end -%>
<% else -%>
Confirm your sweeping alert

We received a request to subscribe <%= @email %> to street sweeping alerts for <%= @formatted_address_area %>.
<% end %>
Confirm your subscription<%= 's' if @neighbor_areas.any? %>: <%= @confirmation_url %>

We'll email you the day before each sweep. No spam, no account needed.

Didn't request this? You can ignore this email and nothing will happen.
<%= render "shared/email_footer", disclaimer: true, coffee_link: false %>
