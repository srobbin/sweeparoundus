<%= render "shared/address_chip", street_address: @street_address, area: @area -%>

The <%= Time.current.year %> sweeping schedule is live.
<% if @first_sweep_dates.any? -%>

Your first sweep is <%= @first_sweep_dates.first %><% if @first_sweep_dates.size > 1 %>, continuing into <%= @first_sweep_dates[1..].to_sentence(two_words_connector: " and ", last_word_connector: ", and ") %><% end %>.
<% else -%>

Your subscription has been carried over for the <%= Time.current.year %> season.
<% end %>
Moved recently? Update your subscription so you keep getting alerts for the right address.

Manage your subscriptions: <%= @manage_url %>

View your <%= Time.current.year %> schedule: <%= area_url(@area) %>
<%= render "shared/email_footer",
      disclaimer: true,
      manage_url: @manage_url,
      unsubscribe_url: @unsubscribe_url,
      coffee_context: :annual_schedule_live %>
