<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "inter-font" %>
    <%= stylesheet_link_tag "tailwind" %>
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= stimulus_include_tags %>
    <script>
      // Global callback for Google Maps loading
      window.initGoogleMaps = function() {
        // Dispatch event to notify Stimulus controllers that Google Maps is ready
        document.dispatchEvent(new CustomEvent('google-maps-loaded'));
      };
    </script>
    <script async defer src="https://maps.googleapis.com/maps/api/js?key=<%= ENV["GOOGLE_MAPS_FRONTEND_API_KEY"] %>&libraries=places&loading=async&callback=initGoogleMaps"></script>
    <%= javascript_include_tag "turbo", type: "module-shim" %>
    <%= render "shared/analytics" if Rails.env.production? && ENV["GOOGLE_ANALYTICS_ID"].present? %>
    <%= yield :head %>
  </head>

  <body class="flex-col text-gray-700 lg:flex">
    <%= yield %>

    <footer>
      <div class="p-4 max-w-6xl mx-auto text-sm text-center lg:flex border-t">
        <div class="flex flex-col mb-4 underline text-gray-600 hover:text-black gap-2 lg:mb-0 lg:flex lg:flex-row lg:gap-6">
          <%= link_to "Home", root_path %>
          <%= link_to "About", about_path %>
          <%= link_to "Privacy Policy", privacy_policy_path %>
          <%= link_to "GitHub", "https://github.com/srobbin/sweeparoundus" %>
        </div>

        <div class="flex-1 lg:text-right">
          &copy; <%= Time.current.year %> <%= ENV["SITE_NAME"] %>
        </div>
      </div>
    </footer>
  </body>
</html>
